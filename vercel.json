{
  "buildCommand": "cd app && npm install && npm run build",
  "outputDirectory": "app/.next",
  "devCommand": "cd app && npm run dev -- --port $PORT",
  "installCommand": "echo 'install handled in buildCommand'"
}
